awsRegion: ${aws_region}

autoDiscovery:
  clusterName: ${eks_cluster_id}
extraArgs:
  aws-use-static-instance-list: true

image:
  tag: ${image_tag}

resources:
  limits:
    cpu: 50m
    memory: 250Mi
  requests:
    cpu: 25m
    memory: 250Mi
